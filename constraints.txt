# Pins for the ML stack that produced and was verified against the committed
# model (models/xgb_best.ubj). Bump these together, retrain, and regenerate
# requirements.lock:
#
#   uv pip compile requirements.txt -c constraints.txt \
#       --python-version 3.11 --python-platform linux -o requirements.lock
numpy==2.3.5
pandas==2.2.3
scipy==1.16.3
scikit-learn==1.8.0
xgboost==3.2.0
shap==0.50.0
optuna==4.6.0
