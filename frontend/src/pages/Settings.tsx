import { CenteredState } from "@/components/CenteredState";

export function SettingsPage() {
  return <CenteredState title="Settings coming next" description="This route is reserved for model, threshold, and environment settings in a later iteration." />;
}
