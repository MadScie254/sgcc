import { Panel } from "@/components/ui/primitives";

export function UploadPage() {
  return (
    <Panel className="p-5">
      <h3 className="font-display text-2xl font-semibold">Dataset upload</h3>
      <p className="mt-3 text-sm text-gp-text-muted">CSV preview and batch prediction upload will land here in the next iteration.</p>
    </Panel>
  );
}