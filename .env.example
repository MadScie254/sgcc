ENV=development
# Required when ENV is not development. Enter the same value on the dashboard Settings page.
API_KEY=replace-me
# Minutes between automatic scoring runs (0 = only at startup).
SCORING_INTERVAL_MINUTES=0
# Largest accepted upload in MB.
MAX_UPLOAD_MB=25
